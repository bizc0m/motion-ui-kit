# Motion UI Kit — RESTORE

**Objectif :** Kit UI Motion — 38 composants, 30 sections, 6 menus, 15 effets.

**Dossier :** `/Users/JOB/#DEV/01-projets/_applications/motion-ui-kit/`

**Commande de reprise :**
```bash
cd "/Users/JOB/#DEV/01-projets/_applications/motion-ui-kit/" && git status --short --branch && npm run build
```

**Tests de santé :**
- `npm run build` doit passer sans erreur TS
- `npm run preview` puis vérifier visuellement la page

**Derniers commits :** à vérifier avec `git log --oneline -5`

**Prochaine action :** ajouter tests visuels ou nouvelles variantes si demandé
